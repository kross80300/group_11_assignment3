Open the project file and click on the game1 C# file, then click 'Run' to start

Required files("allwords.txt", "uniquewords.txt", and "wordfrequency.txt") have already been generated. 
If they are missing open the python script "exctract_words.py" and run to generate files.
